import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'uk.co.codeng',
  appName: 'capacitor-react',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
